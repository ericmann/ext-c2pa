# Template configuration for this extension — consumed by ext-template's
# bin/sync (run from a sibling checkout of DisplaceTech/ext-template):
#
#   ../ext-template/bin/sync .
#
# Managed files (Makefile, build.rs, about.*, rust-toolchain.toml, the
# workflows) re-render from this file; everything else is extension-owned.
# House deviation from the template: namespace is Automattic\VIP\C2PA and
# the license is GPL-2.0-or-later (see README.md) — those live in
# seed-owned files, not here.

NAME=c2pa
NAMESPACE=C2pa
DESCRIPTION=PHP 8.3+ native C2PA Content Credentials: read/validate manifests and sign Media Library images incl. derivatives.
DOCS_HOST=c2pa.eamann.com
STUBS_FILE=c2pa.stubs.php
APT_DEPS=build-essential libclang-dev
BREW_INSTALL=true
X86_RUSTFLAGS=
