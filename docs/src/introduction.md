# ext-c2pa

PHP 8.3+ native C2PA Content Credentials: read/validate manifests and sign Media Library images incl. derivatives.

Documentation lives at []() once Pages + the
CNAME are configured (see `.github/workflows/docs.yml` for the one-time
setup checklist). Grow this book following ext-infer's structure:
getting-started / guide / reference.
